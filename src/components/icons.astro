---
/**
 * Reusable icon components — hand-tuned SVGs that match the brand's premium
 * editorial register. Mario flagged the Unicode ★ and the SVG <line>+
 * pattern as low-budget, so these replace them.
 *
 * Each icon takes a `size` (px) and inherits `currentColor` so it tints with
 * Tailwind text-* classes.
 */
---

<!-- This file's only role is to host SVG path data. The actual usage is
     via the named exports below in Astro / React. -->
